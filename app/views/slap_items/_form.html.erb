<%= form_for(@item,url: slap_item_create_path) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, 'error') %> prohibited this status from being saved:</h2>
      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label t(:item_title) %><br>
    <%= f.text_field :item_title %>
  </div>
  <div class="field">
    <%= f.label t(:item_description)  %><br>
    <%= f.text_area :item_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
